# Level 0 Foundation

Level 0 is the mandatory CSE foundation workspace. The curriculum is seeded separately from the existing AI Roadmaps feature so the existing roadmap experience remains intact.

Progress in this first implementation is intentionally isolated. The final authoritative Progress + REP API should replace client-only persistence before Level 0 completion is used to grant access to later levels.
